# Programa que solicite al usuario los datos para calcular el área de un Cuadrado (■),
# finalmente mostrar el resultado en pantalla.

#def calcular_area_cuadrado(lado):
#    return lado ** 2  # Elevamos el lado al cuadrado

#try:
#    lado = float(input("Introduce la longitud del lado del cuadrado: "))
    
#    if lado <= 0:
#        print("Por favor, introduce un número positivo para la longitud del lado.")
#    else:
#        area = calcular_area_cuadrado(lado)
#        print(f"El área del cuadrado con lado {lado} es: {area}")

#except ValueError:
#    print("Entrada no válida. Introduce un número positivo.")


########################################################################################################################

